import React from 'react';

const PrivatePage = () => {
    return (
        <h1>
            This is a page accessible by PrivateRoute only for authenticated
            users
        </h1>
    );
};

export default PrivatePage;
